{
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/server" }
  ]
}

{
  "builds": [
    {
      "src": "server.py",
      "use": "@vercel/python"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "server.py"
    }
  ]
}
